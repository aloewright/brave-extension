interface:
  display_name: "Brave Extension"
  short_description: "Repo-specific patterns and workflows for brave-extension"
  default_prompt: "Use the brave-extension repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true