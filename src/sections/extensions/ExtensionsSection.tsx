import { ComingSoon } from "../ComingSoon"

export function ExtensionsSection() {
  return (
    <ComingSoon
      title="Extensions"
      milestone="M1 — pending merge from lean-extensions"
      blurb="Extension manager, profiles, and groups merged in from lean-extensions. Merge is tracked under M1; UI lands once chrome.management permission is approved on first reload."
    />
  )
}
