import { ComingSoon } from "../ComingSoon"

export function LibrarySection() {
  return (
    <ComingSoon
      title="Library"
      milestone="M1 — pending merge from lean-extensions"
      blurb="Bookmarks, collected links, captures, and recordings live here. Bookmarks pulled from chrome.bookmarks, links/captures merged in from lean-extensions."
    />
  )
}
